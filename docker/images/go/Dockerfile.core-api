FROM alpine:latest

# Install ca-certificates for HTTPS requests
RUN apk --no-cache add ca-certificates

WORKDIR /app

# Copy pre-built binary
COPY bin/core-api /app/core-api

# Make it executable
RUN chmod +x /app/core-api

EXPOSE 8080

CMD ["./core-api"]

